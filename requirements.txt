fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
pypdf>=4.0
httpx>=0.27
supabase>=2.6

# /kit shared components, vendored into ./vendor so this app deploys standalone.
# Canonical source lives at /kit/py in the private monorepo; re-sync on change
# (see DEPLOY.md). Kept non-editable so the deploy build is reproducible.
./vendor/kit-logger
./vendor/kit-claude
